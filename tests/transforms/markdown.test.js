const test = require('pico-check');
const Style = require('../../lib/transforms/markdown.transform.js');

const opts = {
	build:'./tests/build',
	entry : {
		dir : __dirname,
		name : 'markdownTest'
	}
};


test.skip()('Returns a renderable react component from markdown', (t)=>{


});

test.skip()('', (t)=>{


});


module.exports = test;